#!/bin/bash
DIR="$(cd "$(dirname "$0")" && pwd)"
LOG="$HOME/.config/vox/daemon.log"
mkdir -p "$(dirname "$LOG")"
export OPENAI_API_KEY="${OPENAI_API_KEY:-$(security find-generic-password -a "$USER" -s "vox-openai-api-key" -w 2>/dev/null)}"
exec "$DIR/vox" daemon >> "$LOG" 2>&1
